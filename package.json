{
  "name": "cloud-resume-project",
  "version": "1.0.0",
  "description": "Cloud Resume Challenge Project",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "dev": "python -m http.server 8000 --directory src",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["resume", "cloud", "aws"],
  "author": "Samuel Adusei-Boateng",
  "license": "MIT"
}
